require 'spec_helper'

describe Pronto::Swiftlint do
  it 'has a version number' do
    expect(Pronto::Swiftlint::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
